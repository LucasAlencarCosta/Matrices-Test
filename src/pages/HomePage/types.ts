export interface HomePageProps {
  // Add any props that HomePage might receive in the future
}

export interface HomePageData {
  // Add any data structures specific to HomePage
  title?: string;
  subtitle?: string;
}

// Add other types specific to HomePage as needed
export type HomePageVariant = "default" | "minimal" | "detailed";
